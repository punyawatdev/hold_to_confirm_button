const animationDuration = Duration(milliseconds: 150);

enum HoldToConfirmButtonSize {
  xs,
  small,
  medium,
  large,
}

enum LoadingState {
  init,
  holding,
  done,
}

enum PadState {
  init,
  holding,
  done,
}
